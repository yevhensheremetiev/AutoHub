PORT=3005
DATABASE_URL=
# Auth
JWT_SECRET=
CLIENT_ORIGIN=
# Set to true behind one reverse proxy so rate limits use the real client IP (X-Forwarded-For).
# TRUST_PROXY=

# Firebase Admin (use ONE of the options below)
# Option A: JSON string (recommended for platforms where mounting a file is hard)
FIREBASE_SERVICE_ACCOUNT_JSON=
# Option B: path to service account json file
GOOGLE_APPLICATION_CREDENTIALS=

# SMTP
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=