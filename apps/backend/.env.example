DATABASE_URL="postgresql://postgres:postgres@localhost:5432/autohub?schema=public"

# Auth
JWT_SECRET="change-me-in-production"
CLIENT_ORIGIN="http://localhost:5173"

# Firebase Admin (будь-який один із варіантів):
# 1) ADC / GOOGLE_APPLICATION_CREDENTIALS (рекомендовано для dev)
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"
#
# 2) JSON напряму в ENV (зручно для деплою)
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account", ...}'

