DATABASE_URL=
# Auth
JWT_SECRET=
CLIENT_ORIGIN=
# Set to true behind one reverse proxy so rate limits use the real client IP (X-Forwarded-For).
# TRUST_PROXY=

GOOGLE_APPLICATION_CREDENTIALS=

# SMTP
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=